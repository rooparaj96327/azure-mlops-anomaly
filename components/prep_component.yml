$schema: https://azuremlschemas.azureedge.net/latest/commandComponent.schema.json
name: prep_data
display_name: Preprocess Data Component
type: command
version: 1
description: Preprocess raw dataset by dropping columns and cleaning.

inputs:
  input_data:
    type: mltable   # CHANGED from uri_file → mltable

outputs:
  output_data:
    type: mltable

code: ../src
environment: azureml:AzureML-sklearn-1.3:1

command: >-
  python prep.py
  --input_data ${{inputs.input_data}}
  --output_data ${{outputs.output_data}}
