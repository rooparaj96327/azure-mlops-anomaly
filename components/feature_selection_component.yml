$schema: https://azuremlschemas.azureedge.net/latest/commandComponent.schema.json
name: feature_selection
display_name: Feature Selection Component
type: command
version: 1
description: Selects relevant features using correlation or other methods.

inputs:
  processed_data:
    type: mltable   # CHANGED from uri_file → mltable

outputs:
  selected_data:
    type: mltable

code: ../src
environment: azureml:AzureML-sklearn-1.3:1

command: >-
  python feature_selection.py
  --processed_data ${{inputs.processed_data}}
  --selected_data ${{outputs.selected_data}}
